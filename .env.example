# Kopieren nach .env und bei Bedarf anpassen.
# Zentraler Zugriffspunkt (NGINX Loadbalancer, HTTPS) -> https://localhost:<PORT>

# Host-Port des Loadbalancers (Single Point of Access, TLS/HTTPS)
LOADBALANCER_PORT=8443
